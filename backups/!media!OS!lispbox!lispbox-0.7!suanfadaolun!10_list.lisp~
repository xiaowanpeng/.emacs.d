(defstruct node pre value next)

(defparameter m-list (make-node :pre nil :value nil :next nil))

(defun list-insert (l n)
  (setf (node-pre n) l
        (node-next n) (node-next l)
        (node-next l) n)
  (if (not (node-next n))
      (setf (node-pre (node-next n)) n)))
