(in-package "COMMON-LISP-USER")

(defpackage cpu
  (:use common-lisp)
  (:export run))

(defpackage bios
  (:use common-lisp)
  (:export run))

(defpackage disk
  (:use common-lisp)
  (:export get-first-sector))

(defpackage boot
  (:use common-lisp)
  (:export run))

(defpackage protect
  (:use common-lisp))
