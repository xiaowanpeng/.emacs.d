########################################################
#
# kernel 层 Makefile 的写法
# 注意：
#     1、kernel 层的 OBD 一般是不 install 的
#     2、kernel 层一般也不定义 DEST_SOURCES，因为这层只是提供基本的 db 类定义
#
########################################################

# 包含基本的系统参数定义【不能更改】
include $(OB_REL)/etc/NGbasedefine.mk

########################################################

# 目标名称，不用带 lib 的前缀或者 D.so 的后缀，最后生成的文件名是 lib$(DEST)_kernel$(DLLTAIL)【必须修改】
DEST = FlowManager

# 目标的类型，必须是 KERNEL【不能修改】
DEST_TYPE = KERNEL

# 目标程序实现的 OBD 名称【必须修改】
IMPL_OBD = FlowManager_db.obd

# 使用的数据库连接串【必须修改】
OBD_DBCONN=aijh2

#USER_DEFS = __INC_SYSMGNT__ $(ACCT_VER)
########################################################

# 包含基本的 Makefile 规则文件【不能更改】
include $(OB_REL)/etc/NGCmake_au
